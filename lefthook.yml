pre-commit:
  commands:
    eslint:
      root: server
      glob: "*.{js,jsx}"
      run: npx eslint --fix {staged_files}
    prettier:
      root: server
      glob: "*.{js,jsx,json,css,md}"
      run: npx prettier --write {staged_files}
